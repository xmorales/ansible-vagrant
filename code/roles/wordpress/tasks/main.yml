---
- name: Install Requirements
  yum: name={{ item }}
        state=present
        disablerepo=*
        enablerepo=base
  with_items:
    - httpd
    - MySQL-python
  tags: deployment

# - name: Get wikimedia
#   get_url: url=http://releases.wikimedia.org/mediawiki/1.24/mediawiki-{{ wiki_version }}.tar.gz
#            dest=/var/www/html/
#   tags: deployment

# - name: Get wikimedia
#   # shell: cd /var/www/html/; mkdir wiki; curl {{ wiki_path }} | tar xvz --directory=wiki/
#   command: chdir=/var/www/html/ /bin/tar xvf mediawiki-{{ wiki_version }}.tar.gz
#             creates=/var/www/html/wiki
#   register: wiki_result
#   failed_when: wiki_result.rc != 0
#   tags: deployment

- name: Download WordPress
  get_url: url=http://wordpress.org/wordpress-{{ wp_version }}.tar.gz dest=/srv/wordpress-{{ wp_version }}.tar.gz
           sha256sum="{{ wp_sha256sum }}"

- name: Extract archive
  command: chdir=/srv/ /bin/tar xvf wordpress-{{ wp_version }}.tar.gz creates=/srv/wordpress

- name: Add group "wordpress"
  group: name=wordpress

- name: Add user "wordpress"
  user: name=wordpress group=wordpress home=/srv/wordpress/

- name: Fetch random salts for WordPress config
  local_action: command curl https://api.wordpress.org/secret-key/1.1/salt/
  register: "wp_salt"
  sudo: no

- name: Create WordPress database
  delegate_to: database1.vagrant.dev
  mysql_db: name={{ wp_db_name }} state=present
  tags: mysql

- name: Create WordPress database user
  delegate_to: database1.vagrant.dev
  tags: mysql
  mysql_user: name={{ wp_db_user }}
              password={{ wp_db_password }}
              priv={{ wp_db_name }}.*:ALL
              host='webserver1.vagrant.dev'
              state=present

- name: Copy WordPress config file
  template: src=wp-config.php dest=/srv/wordpress/

- name: Copy Apache virtualhost
  template: src=wordpress.conf dest=/etc/httpd/conf.d/
  notify: reload httpd

- name: Change ownership of WordPress installation
  file: path=/srv/wordpress/ owner=wordpress group=wordpress state=directory recurse=yes
