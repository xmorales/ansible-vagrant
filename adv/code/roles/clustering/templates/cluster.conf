<?xml version="1.0"?>
<cluster config_version="1" name="{{ clustering.name }}">
  <logging debug="{{ clustering.debug }}"/>
  <clusternodes>
    {% for host in groups['web'] %}
    <clusternode name="{{ hostvars[host]['ansible_hostname'] }}" nodeid="{{ loop.index }}"/>
    {% endfor %}
  </clusternodes>￼￼
</cluster>
